name: p0-14-live-post-merge

on:
  push:
    branches:
      - acp/p0-14-live-20260814-090409-98629

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Prove the disposable merge SHA reached Actions
        run: test -n "$GITHUB_SHA"
