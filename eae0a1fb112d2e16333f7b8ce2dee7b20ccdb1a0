Limit: this deletion is a condition being met, not a fresh judgement: the row guarding the declaration named its own exit — both go when src/ceo/cto-binding-delegation.ts stops being the consumer — and that happened when the durable grant was removed
Warn: OwnerApprovalReceipt itself stays, and so does the owner decision path that reads it; what goes here is the copy of its strip-normal form that lived in the attachment module, whose last reader was the test asserting the copy's keys matched the interface
Blast: local
Record-Id: r-3cf54c21a095
Provenance: inherited ebbd3661b2175e6b6e7892313b893713902865f5
