# Progress record — 2026-08-14

Written for whoever reads next, including me after a context compaction. Conversation is not
durable; this file is. Updated as work lands.

## Where the release actually stands

**The critical path is #241, the observation window** — 30 lifecycles is elapsed time and
cannot be shortened by coding. Everything else is either done, parallel, or shorter.

**#241 is blocked on #419, and that is a change from the roadmap as given.** The roadmap put
starting the clock in Phase 1 and reviewer isolation in Phase 2. Driving the real lifecycle
tonight showed the dependency runs the other way.

### The lifecycle blocker chain, measured rather than estimated

Run: `ACP_COMPONENT_INTEGRATION=1 pnpm test:e2e` from `main`.

| # | blocker | status |
|---|---|---|
| 1 | `WRITE_REQUIRES_MANAGED_RUN` at `projects.register` | fixed — `manifestAuthorizationForTests` + `allowTestEvidenceWriters` |
| 2 | `CAPACITY_UNKNOWN_NOT_ROUTABLE` at `runs.dispatch` | fixed — refresh, then an operator observation |
| 3 | `REVIEW_UNAVAILABLE` / `ISOLATION_LOST` at blind review | **open — #419** |

Blind review is *inside* the lifecycle. No lifecycle completes until reviewer isolation
attests on this host, so **#241's counter cannot leave zero until #419 closes.** Registering
three dogfood projects first would produce three projects and zero lifecycles.

Blocker 2 is worth keeping: the collector genuinely runs, invokes the Claude CLI's interactive
`/usage`, finds no parseable window, and writes `sensorHealth: ERROR` over the seeded file.
That is #424's scenario exactly, and the operator observation is the mechanism built for it.
The observation is authored by the `cli` owner this host declares, never an actor the test
invents.

### Revised order

1. **#419** — positive provider-reachability proof. Gates everything below.
2. **#243 + #245 together** — one live Buzz scenario carrying one owner decision.
3. **#240** — two-repository merge. Execution, not code; owner blocker already cleared.
4. **Start the clock** — three dogfood projects, first lifecycle, window start time recorded
   in `docs/ACCEPTANCE.md`.
5. **#392**, then A1/A2 (#448 → #449 → #450 → #451) in the window's shadow.

No release date can be given yet. Zero lifecycles have completed, so there is no
lifecycles-per-day figure to extrapolate from. Quoting one now would be an estimate dressed as
a measurement, which is the failure `docs/TERMINOLOGY.md` §6 and #448 are both about.

## Waiting on the owner

Nothing is blocked on the owner *except* these. Prepared up to the line and stopped.

| item | what is needed |
|---|---|
| v1.0.0 tag and release | Owner approval. Irreversible external publication. |
| #392 | A live Telegram bot token. The ingress server itself exists as of `8461d82`. |

## Today, for the record

Six lanes opened the day as DO NOT MERGE. All merged, plus ten more PRs.

Four contract documents were left behind, and they outlast the code:
`docs/TERMINOLOGY.md` (+ `scripts/verify-terminology.mjs`), the 29 annotated database triggers
in `src/db/schema.sql`, the narrowed absorption list, and the gate map in `docs/STATUS.md`.

### The method that produced most of the findings

Read the code, then **mutate it and re-run**. Four times today reading said "fixed" and
mutation said "unproven":

- the reviewer transcript deny — 808 tests passed with it removed
- the withheld manifest — nothing tested it at all
- the positive egress probe — 818 tests passed with it hardcoded
- the daemon's Buzz delivery timer — 820 tests passed with it deleted

Two held up under the same treatment (P1-15, P1-06), which is what makes the other four
meaningful rather than a story about a bad suite.

### Corrections recorded, so they are not re-derived

- **#461** — `verify-r2 #164` is flaky on CI. An identical re-run with no change passed. My
  earlier claim on #439 that a rebase fixed it was wrong; I fitted an explanation to one
  observation.
- **#443 disposition** — a `### Fixed` heading in a progress comment reads as a recorded
  disposition to `ssot-report.mjs`, which flagged it. A partial result must not be written in
  the vocabulary of a completed one.
- **The opt-in e2e had stopped working**, not merely gone unverified as `docs/STATUS.md` said.
  It failed at the first step and nothing reported it because nothing ran it.
