# ACP state — 2026-08-15 (updated)

Written so the next reader does not have to reconstruct it from a conversation. Everything
below is either verified or explicitly marked unknown.

## Where the release stands

Critical path is **#241, the observation window**. 30 lifecycles is elapsed time; it cannot be
shortened by coding, only started earlier.

**Blocked on one thing: the reviewer credential**, which needs the owner's interactive OAuth.
Every other prerequisite is cleared.

## Daemon — installed, not started

- Launcher and plist rendered with `--no-start`. **Nothing is running.**
- Install needs, in order: a build (`dist/daemon/agentcpd.js`), then `ACP_MCP_TOKEN` and
  `ACP_OPERATOR_TOKEN` in the Keychain — the installer validates these, not just the launcher.
  Provisioned per the documented `openssl rand -hex 32` convention; they are daemon-internal
  tokens, not owner secrets.
- `~/.agent-control-plane/agentcpd-launch.sh:94` derives `ACP_CLAUDE_REVIEWER_CONFIG_DIR`.
  Verified in the rendered launcher, not the template.

### Overlap check before starting — all three answered safe

1. **Does start spawn actors immediately?** `start()` runs `reconcile()`, which resumes runs and
   rebinds assignments. The state database is **empty** — 0 projects, 0 runs, 0 ACTIVE
   assignments, 0 sessions — so there is nothing to resume. Note `state.sqlite` *exists* (dated
   Aug 12); "no daemon has run" and "the database is empty" are different facts and both were
   checked.
2. **Repository overlap?** None, because no repository is registered. This becomes live the
   moment a dogfood project is registered.
3. **What serialises if it did?** `resource_claims` partial unique indexes on
   `(repository_identity, worktree_id)` and `(repository_identity, branch)` where
   `status = 'HELD'`. Enforced by the database, live now, not waiting on A2.

### Why the daemon is required at all

`tests/e2e/real-component-integration.test.ts:123` builds its state root with `tempDir()`. Every
run writes a fresh `state.sqlite` and discards it — thirty runs give thirty databases, not
thirty counted lifecycles. #241 reads its count from `telemetry_metrics`, so it needs one
persistent deployment.

## Dogfood repositories — proposed, none registered

Occupied and excluded: `agent-control-plane` (this session), `commitlore` (26846, IMMUTABLE),
`logic-pro-mcp` (64972, IMMUTABLE), `agent-operator-score` (34861), `stock-ai-newsletter`
(another session's branch).

Clean candidates, verified with no process holding a cwd there and no uncommitted work:
**`next15-echo`**, **`polaris`**.

A third is undecided. `claude-code-mastery` was proposed and should be dropped — last touched
2026-03-31, so a dogfood run there measures a dormant tree. `cl-e2e*` are CommitLore's e2e
fixtures and belong to 26846.

## A0 residuals

| issue | state | next step |
|---|---|---|
| #440 | prose corrected (#468); ACCEPTANCE and README no longer claim the deployment's allowlist | route `capture-buzz-live.ts` through `configuredBuzzActorIngressPolicy` — that is #243's acceptance |
| #442 | open, untouched | three telegram gaps: DIRECT never reaches Hermes; older forward markers read as live commands; prompts go only to `allowedChatIds[0]` |
| #443 | 7 of 9 fixed | two named with reproductions, below |
| #454 | open | dispatch staleness boundary (5m) and sensor tick (4m) are unrelated constants; changing one silently changes the other's meaning |
| #461 | open | `verify-r2 #164` flaky on CI — three occurrences across three unrelated branches, passes on re-run |

### #443's two remaining, with reproductions

- **`deploy-launchd` export**: restore `expect(resolvedBuzz).toBe(buzzPath)` in *"#423 gives the
  daemon an absolute path…"* — it reports `<unset>` even though `ACP_RESOLVED_BUZZ_BINARY` is
  baked and the guarding condition should hold. Stopped at three attempts.
- **`verify-hardening` identity branch**: disabling
  `markContainmentLost("candidate identity was not captured…")` leaves the outcome
  byte-identical, because a `ps` broken for the whole run already fails the cleanup path
  upstream. The current test guards the fail-closed result, not the reason.

## A1 (#448) — where to start

Two pieces that do not touch `Decision<T>`:

1. A STALE reason-code family in `reason-codes.ts` — additive, no call-site impact, and
   `verify-reason-codes.mjs` already makes published codes append-only.
2. Evidence files carrying their target commit SHA, marked STALE when it differs from HEAD.
   This is CP-HI-06 generalised outside the run lifecycle; the mechanism exists and works, it
   just only lives inside a run.

**Do not widen `Decision<T>` to three states.** Measured: `.allowed` is read 1557 times, 356 of
them as `if (!x.allowed)`. A third state leaves all 356 compiling and silently folding — the
exact bug the rule exists to prevent, at 1557 sites, with the compiler unable to help. The
proposal in #448 is staleness as a *precondition*: a comparison either has both generations
bound and returns today's two-way `Decision`, or refuses to return one at all.

## Owner queue

| item | needs |
|---|---|
| reviewer credential | `CLAUDE_CONFIG_DIR="$HOME/.agent-control-plane/reviewer/claude" claude` then `/login`. Gates #241. |
| #392 Telegram | a live bot token; ingress server exists as of `8461d82` |
| v1.0.0 tag | after the window's five counts are in `docs/ACCEPTANCE.md` |

Full procedures with verification commands: `docs/ops/owner-actions.md`.


---

# Update — after the A0 and A1 work

## What closed since the first version

**#442 fully closed**, all three Telegram gaps, each mutation-verified:
- forward detection read one marker of five, so a forwarded `/managed …` created a run
- the DIRECT reply named Hermes as recipient when Hermes never saw the message
- owner-gate prompts went only to `allowedChatIds[0]`, leaving a second allowlisted chat unable
  to answer at all

**A1's first two items landed.** `STALENESS_REASON_CODES` classifies the eleven staleness codes
that already existed; `verify-evidence-freshness` reports CURRENT / STALE / UNDECLARED and found
**52 of 52 committed evidence files stale**.

**#443's `deploy-launchd` question resolved** — and it was a production defect.
`optional_keychain_value` unsets each account before looking it up, which destroyed the
`ACP_BUZZ_BINARY` the launcher had resolved from the install-time path. The daemon then fell
back to bare `buzz`, unreachable from its pinned PATH: #423's failure reintroduced by a later
line in the same file. Removing the `unset` is the wrong fix — it is what stops an inherited
token passing for a provisioned one. Resolving after the loop keeps both.

## Still open, with reproductions

| item | state |
|---|---|
| #461 | probe armed (#479, draft, **not for merge**); three CI runs, three passes, no failing instance caught |
| #443 | `verify-hardening` identity branch — the test guards the fail-closed result, not the reason |
| #440 | prose corrected; the capture itself still needs `configuredBuzzActorIngressPolicy` |
| #454 | dispatch boundary (5m) and sensor tick (4m) are unrelated constants |

## Two decisions waiting on the 총괄

1. **A1 item 3.** As written it presumes a uniformity the code does not have — 12 staleness raise
   sites carry both sides under recognisable names, 65 carry something else, 10 carry no object.
   A naming-based check fires on 65 mostly-fine sites and gets switched off. Proposed instead: a
   staleness denial must *record* both sides, enforceable at the 22 sites carrying no pair.
2. **The third dogfood repository.** `next15-echo` and `polaris` verified clean — no process
   holding a cwd, no uncommitted work. `claude-code-mastery` was proposed and withdrawn: dormant
   since 2026-03-31.

## What I got wrong today, so it is not re-derived

- **#461, twice.** Diagnosed a one-second deadline from a timing-shaped failure without running
  the check that refutes it; then generalised from local mutations plus four CI failures to "the
  fence does not work on CI", which a passing CI run contradicted. Also quoted "one in three" as
  a rate from a sample that counted failures and not passes.
- **Three of four mutations on that test were invalid** — they changed what a function *reported*
  while the kill calls still executed. Only returning before any signal disabled the behaviour.
- **Two disposition markers.** A `### Fixed` heading on #443 and a `## FIXED` on #461 both read
  to `ssot-report` as the tracker record that a finding was resolved. Both were caught by the
  reconciler and both were wrong.

The common thread is worth keeping: every one was a single available explanation adopted without
seeking the observation that would refute it. That is the same defect #448 exists for, and it
does not stop applying because the subject is a test rather than a run.


---

# Update — codex reviewer switch (in progress)

## Why codex and not Claude

The constraint was never "the reviewer must be Claude". It is **"the reviewer must attest
isolation"**, and only two of three adapters can:

```
cli-adapters.ts:1331  ClaudeCliAdapter  supportsReviewerIsolation = true
cli-adapters.ts:1635  CodexCliAdapter   supportsReviewerIsolation = true
cli-adapters.ts:1944  GrokCliAdapter    supportsReviewerIsolation = false
```

**grok was proposed as the looser alternative and refused on that line.** If someone proposes
it again to unblock a login, this is where it fails — not a preference, a declared capability.
Loosening the path is not the same as dropping the guarantee.

Claude's reviewer path needs an interactive OAuth login that cannot be completed away from the
machine; three attempts today left `~/.agent-control-plane/reviewer/claude/` with a
`.claude.json` and no `.credentials.json`. **The Claude path is kept as an option, not deleted.
codex is the default.**

## What codex gives

```
~/.codex/auth.json                        4132 B, mode 600 — real, no login needed
CodexCliAdapter.supportsReviewerIsolation = true
requiresReviewerProviderSessionProof      = true   ← stricter than the Claude path
ACP_CODEX_REVIEWER_HOME                   already derived by the launcher (9e970f2)
```

## Isolation is unchanged, and this is the part not to shortcut

The reviewer does **not** get `~/.codex` — that tree holds producer conversation state
(`sessions/`, `attachments/`, `browser/`). It gets
`~/.agent-control-plane/reviewer/codex/` containing **`auth.json` and nothing else**, mode 600
in a 700 directory. Provisioned by file copy, which is why no owner is required.

## Where it stands

Done: scope provisioned; `tests/e2e/real-component-integration.test.ts` now builds a
`CodexCliAdapter` with that credential dir and the egress config, and asks for
`provider: "gpt", model: "gpt-5.6-sol"`.

**Next step — the current blocker.** The run reaches review and stops at:

```
REVIEW_UNAVAILABLE  reasonCode: CAPACITY_UNKNOWN_NOT_ROUTABLE
```

The e2e seeds and observes capacity for **claude only**. The reviewer is now **gpt**, which has
no reading, so review cannot be admitted. Fix: seed `capacity/gpt.json` and add a second
operator observation for provider `gpt`, mirroring the claude one at the same call site. That
is the same mechanism #424 built — the collector cannot read quota on this host, so an
authenticated operator observation carries it.

## Branch protection — done and proven

Applied: `contexts=["verify"]`, `strict=true`, `enforce_admins=true`, `reviews=0`.

Proven by behaviour, not by the settings page: a throwaway PR read `BLOCKED` with no checks,
then `CLEAN` once `verify` went green, then merged. It gates and it releases.

Revert (restores the state captured before applying — a plain DELETE would also clear
force-push and deletion blocking, which were already on):

```bash
gh api repos/MongLong0214/agent-control-plane/branches/main/protection --method PUT --input - <<'JSON'
{"required_status_checks":null,"enforce_admins":false,"required_pull_request_reviews":null,
 "restrictions":null,"allow_force_pushes":false,"allow_deletions":false,
 "required_linear_history":false,"required_conversation_resolution":false}
JSON
```

## A1 item 3 — settled as a ratchet

Measured: **87** staleness raise sites — 12 carry both sides under recognisable names, 65 carry
some other object, 10 carry no object. **75 carry no pair.**

An earlier figure of 22 was wrong: it was 12+10, which double-counts the compliant 12 into the
enforcement target. The corrected number weakened the proposal, so the proposal changed rather
than the number being quietly reused.

Agreed shape: the rule applies to **new** staleness denials; the existing 75 is recorded as a
baseline that fails if it grows — same as the buzz-actor rule. Existing sites get fixed when
touched. A 75-site sweep is a separate PR series, deliberately deferred.

## #443 — uncovered, deliberately

The `verify-hardening` identity branch stays uncovered. STATUS.md must record three things, not
one: why (it is a secondary record — a mutation proved the TypeScript layer does not contain a
`setsid` descendant at all; `fence_descendants` does), what reverses it (the TypeScript layer
gaining direct containment), and what to do then.

## Dogfood — three, verified

`claude-code`, `next15-echo`, `polaris` — all `main`, `dirty=0`, no process holding a cwd.
#241's three-repository criterion is met without lowering it. `polaris` was nearly dropped on a
truncated listing; it exists at `~/projects/polaris`.

## Remaining, each with its next step

| item | next step |
|---|---|
| #241 | seed gpt capacity, complete one lifecycle, record window start in ACCEPTANCE.md |
| #240 | same lifecycle blocker; then a two-repository run |
| #243 / #245 | one live Buzz scenario carrying one owner decision — closes both |
| #392 | needs a live Telegram bot token — still owner-only |
| #360 | withheld-list probes done; the egress-optional finding is #419 |
| #461 | quarantined; the containment window is a documented limit |

## What is not known

- Whether codex actually completes a review here. It has never run as the in-run reviewer on
  this host; the capacity blocker is upstream of finding out.
- Whether `#164`'s race has any cause beyond the scan window. Not reproducible locally — 0 of 8
  escaped under the worst case constructible.
- Whether the reviewer egress proxy's positive-reachability path works with codex; it was only
  ever exercised against the Claude endpoint list.
