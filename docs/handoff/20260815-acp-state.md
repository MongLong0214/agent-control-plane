# ACP state — 2026-08-15

Written so the next reader does not have to reconstruct it from a conversation. Everything
below is either verified or explicitly marked unknown.

## Where the release stands

Critical path is **#241, the observation window**. 30 lifecycles is elapsed time; it cannot be
shortened by coding, only started earlier.

**Blocked on one thing: the reviewer credential**, which needs the owner's interactive OAuth.
Every other prerequisite is cleared.

## Daemon — installed, not started

- Launcher and plist rendered with `--no-start`. **Nothing is running.**
- Install needs, in order: a build (`dist/daemon/agentcpd.js`), then `ACP_MCP_TOKEN` and
  `ACP_OPERATOR_TOKEN` in the Keychain — the installer validates these, not just the launcher.
  Provisioned per the documented `openssl rand -hex 32` convention; they are daemon-internal
  tokens, not owner secrets.
- `~/.agent-control-plane/agentcpd-launch.sh:94` derives `ACP_CLAUDE_REVIEWER_CONFIG_DIR`.
  Verified in the rendered launcher, not the template.

### Overlap check before starting — all three answered safe

1. **Does start spawn actors immediately?** `start()` runs `reconcile()`, which resumes runs and
   rebinds assignments. The state database is **empty** — 0 projects, 0 runs, 0 ACTIVE
   assignments, 0 sessions — so there is nothing to resume. Note `state.sqlite` *exists* (dated
   Aug 12); "no daemon has run" and "the database is empty" are different facts and both were
   checked.
2. **Repository overlap?** None, because no repository is registered. This becomes live the
   moment a dogfood project is registered.
3. **What serialises if it did?** `resource_claims` partial unique indexes on
   `(repository_identity, worktree_id)` and `(repository_identity, branch)` where
   `status = 'HELD'`. Enforced by the database, live now, not waiting on A2.

### Why the daemon is required at all

`tests/e2e/real-component-integration.test.ts:123` builds its state root with `tempDir()`. Every
run writes a fresh `state.sqlite` and discards it — thirty runs give thirty databases, not
thirty counted lifecycles. #241 reads its count from `telemetry_metrics`, so it needs one
persistent deployment.

## Dogfood repositories — proposed, none registered

Occupied and excluded: `agent-control-plane` (this session), `commitlore` (26846, IMMUTABLE),
`logic-pro-mcp` (64972, IMMUTABLE), `agent-operator-score` (34861), `stock-ai-newsletter`
(another session's branch).

Clean candidates, verified with no process holding a cwd there and no uncommitted work:
**`next15-echo`**, **`polaris`**.

A third is undecided. `claude-code-mastery` was proposed and should be dropped — last touched
2026-03-31, so a dogfood run there measures a dormant tree. `cl-e2e*` are CommitLore's e2e
fixtures and belong to 26846.

## A0 residuals

| issue | state | next step |
|---|---|---|
| #440 | prose corrected (#468); ACCEPTANCE and README no longer claim the deployment's allowlist | route `capture-buzz-live.ts` through `configuredBuzzActorIngressPolicy` — that is #243's acceptance |
| #442 | open, untouched | three telegram gaps: DIRECT never reaches Hermes; older forward markers read as live commands; prompts go only to `allowedChatIds[0]` |
| #443 | 7 of 9 fixed | two named with reproductions, below |
| #454 | open | dispatch staleness boundary (5m) and sensor tick (4m) are unrelated constants; changing one silently changes the other's meaning |
| #461 | open | `verify-r2 #164` flaky on CI — three occurrences across three unrelated branches, passes on re-run |

### #443's two remaining, with reproductions

- **`deploy-launchd` export**: restore `expect(resolvedBuzz).toBe(buzzPath)` in *"#423 gives the
  daemon an absolute path…"* — it reports `<unset>` even though `ACP_RESOLVED_BUZZ_BINARY` is
  baked and the guarding condition should hold. Stopped at three attempts.
- **`verify-hardening` identity branch**: disabling
  `markContainmentLost("candidate identity was not captured…")` leaves the outcome
  byte-identical, because a `ps` broken for the whole run already fails the cleanup path
  upstream. The current test guards the fail-closed result, not the reason.

## A1 (#448) — where to start

Two pieces that do not touch `Decision<T>`:

1. A STALE reason-code family in `reason-codes.ts` — additive, no call-site impact, and
   `verify-reason-codes.mjs` already makes published codes append-only.
2. Evidence files carrying their target commit SHA, marked STALE when it differs from HEAD.
   This is CP-HI-06 generalised outside the run lifecycle; the mechanism exists and works, it
   just only lives inside a run.

**Do not widen `Decision<T>` to three states.** Measured: `.allowed` is read 1557 times, 356 of
them as `if (!x.allowed)`. A third state leaves all 356 compiling and silently folding — the
exact bug the rule exists to prevent, at 1557 sites, with the compiler unable to help. The
proposal in #448 is staleness as a *precondition*: a comparison either has both generations
bound and returns today's two-way `Decision`, or refuses to return one at all.

## Owner queue

| item | needs |
|---|---|
| reviewer credential | `CLAUDE_CONFIG_DIR="$HOME/.agent-control-plane/reviewer/claude" claude` then `/login`. Gates #241. |
| #392 Telegram | a live bot token; ingress server exists as of `8461d82` |
| v1.0.0 tag | after the window's five counts are in `docs/ACCEPTANCE.md` |

Full procedures with verification commands: `docs/ops/owner-actions.md`.
