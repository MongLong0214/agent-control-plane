# No trailing slash: a `node_modules` symlink must be ignored too, not just a directory.
node_modules
dist/
*.log
.DS_Store
.env
.env.*
!.env.example
evidence/local/
var/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
.acp-secrets/

# A delegate writes its report here; it is never part of the repository.
HANDOFF-REPORT.md
