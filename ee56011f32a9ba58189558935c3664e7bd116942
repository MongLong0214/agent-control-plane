Limit: `KeepAlive { SuccessfulExit = false }` 는 비정상 종료만 재기동합니다.
Limit: `applySchema` 는 온디스크 버전이 빌드보다 높으면 던지므로 **되돌릴 수 없고**, `KeepAlive` 가 30초마다 다시 시도합니다.
Warn: exit 0 은 거부된 데몬이 누가 올릴 때까지 내려가 있다는 뜻이고, 거부를 조용하게 만듭니다
Warn: `approvedBy` 는 인가 검사가 아니라 귀속 기록입니다.
Provenance: reconstructed
